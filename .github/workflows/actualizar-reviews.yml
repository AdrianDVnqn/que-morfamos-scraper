name: Actualizar Reseñas

on:
  schedule:
    # Ejecutar el día 15 de cada mes a las 3:00 AM UTC
    - cron: '0 3 15 * *'
  workflow_dispatch:  # Permite ejecución manual

jobs:
  scrape-reviews:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write
    timeout-minutes: 360  # 6 horas máximo
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Instalar dependencias básicas
        run: pip install requests
      
      # NOTIFICACIÓN DE INICIO
      - name: Notificar inicio
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python -c "
          import os, requests, datetime
          webhook = os.environ.get('DISCORD_WEBHOOK_URL')
          if webhook:
              msg = f'''🚀 **INICIANDO - Actualización de Reseñas**
              📅 {datetime.datetime.now().strftime('%d/%m/%Y %H:%M')}
              
              Comenzando scraping incremental de reseñas...'''
              requests.post(webhook, json={'embeds': [{'description': msg, 'color': 0x9b59b6}]})
              print('✓ Notificación de inicio enviada')
          "
      
      - name: Instalar Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable
      
      - name: Instalar dependencias completas
        run: |
          pip install selenium webdriver-manager beautifulsoup4 psycopg2-binary geopandas
      
      # Descargar datos previos del repo privado
      - name: Descargar datos previos
        env:
          PRIVATE_REPO_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          git clone "https://x-access-token:${PRIVATE_REPO_TOKEN}@github.com/AdrianDVnqn/que-morfamos.git" private-repo
          cp private-repo/data/lugares_validados.csv . 2>/dev/null || echo "No hay lugares_validados.csv"
          cp private-repo/data/estado_reviews.csv . 2>/dev/null || true
          cp private-repo/data/reviews_neuquen.csv . 2>/dev/null || true
      
      # Verificar que existe lugares_validados.csv
      - name: Verificar datos de entrada
        run: |
          if [ ! -f lugares_validados.csv ]; then
            echo "ERROR: No existe lugares_validados.csv"
            echo "Primero ejecutá el workflow 'Actualizar Lugares'"
            exit 1
          fi
          echo "✓ Encontrados $(wc -l < lugares_validados.csv) lugares para procesar"
      
      # Ejecutar scraper de reseñas
      - name: Ejecutar scraper de reseñas
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          set -o pipefail
          mkdir -p logs
          python -u opiniones-scraper.py 2>&1 | tee logs/execution_stdout.log
      
      # Regenerar embeddings con las nuevas reviews
      - name: Regenerar Embeddings
        if: success()
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          pip install langchain-openai langchain-postgres sqlalchemy psycopg2-binary
          python -u regenerate_embeddings.py 2>&1 | tee -a logs/execution_stdout.log
      
      # Subir Artifact de Logs (GitHub)
      - name: Subir Logs de Ejecución
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: logs-actualizar-reviews
          path: logs/
      
      # Subir resultados al repo privado
      - name: Subir resultados al repo privado
        env:
          PRIVATE_REPO_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          mkdir -p private-repo/data
          mkdir -p private-repo/logs
          
          cp reviews_neuquen.csv private-repo/data/ 2>/dev/null || true
          cp estado_reviews.csv private-repo/data/ 2>/dev/null || true
          
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          cp logs/reviews_run.log private-repo/logs/ 2>/dev/null || true
          cp logs/execution_stdout.log private-repo/logs/ 2>/dev/null || true
          cp logs/reviews_run.log "private-repo/logs/reviews_${TIMESTAMP}.log" 2>/dev/null || true
          
          cd private-repo
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add .
          git commit -m "Actualización de reseñas - $(date '+%Y-%m-%d %H:%M:%S')" || echo "Sin cambios"
          git push
      
      # NOTIFICACIÓN DE ÉXITO
      - name: Notificar éxito
        if: success()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python -c "
          import os, csv, requests, datetime
          webhook = os.environ.get('DISCORD_WEBHOOK_URL')
          if not webhook: exit(0)

          if os.path.exists('.continue'):
              msg = f'''🔄 **CONTINUANDO - Workflow Reiniciado**
              📅 {datetime.datetime.now().strftime('%d/%m/%Y %H:%M')}
              
              El tiempo límite fue alcanzado, pero quedan lugares pendientes.
              Se iniciará una nueva ejecución automáticamente.'''
              color = 0x3498db  # Azul
          else:
              total_reviews = exitos = sin_opiniones = errores = 0
              try:
                  with open('reviews_neuquen.csv', 'r') as f:
                      total_reviews = sum(1 for _ in csv.reader(f)) - 1
              except: pass
              try:
                  with open('estado_reviews.csv', 'r') as f:
                      reader = csv.DictReader(f)
                      for row in reader:
                          est = row.get('estado')
                          if est == 'EXITO': exitos += 1
                          elif est == 'SIN_OPINIONES' or est == 'SIN_OPINIONES_DEFINITIVO': sin_opiniones += 1
                          else: errores += 1
              except: pass

              msg = f'''✅ **COMPLETADO - Actualización de Reseñas**
              📅 {datetime.datetime.now().strftime('%d/%m/%Y %H:%M')}

              📝 Total reseñas: **{total_reviews}**
              ✅ Lugares procesados: **{exitos}**
              🚫 Sin opiniones: **{sin_opiniones}**
              ⚠️ Pendientes/Errores: **{errores}**'''
              color = 0x2ecc71  # Verde

          requests.post(webhook, json={'embeds': [{'description': msg, 'color': color}]})
          print('✓ Notificación enviada')
          "

      # AUTO-CONTINUAR SI QUEDAN PENDIENTES
      - name: Auto-continuar si hay pendientes
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ -f .continue ]; then
            echo "⚠️ Quedan pendientes: Reiniciando workflow..."
            gh workflow run actualizar-reviews.yml
          else
            echo "✓ Todo completado. No se requiere reinicio."
          fi
      
      # NOTIFICACIÓN DE ERROR
      - name: Notificar error
        if: failure()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python -c "
          import os, requests, datetime
          webhook = os.environ.get('DISCORD_WEBHOOK_URL')
          if webhook:
              msg = f'''❌ **ERROR - Actualización de Reseñas**
              📅 {datetime.datetime.now().strftime('%d/%m/%Y %H:%M')}
              
              El workflow falló inesperadamente. Revisar logs.'''
              requests.post(webhook, json={'embeds': [{'description': msg, 'color': 0xe74c3c}]})
          "
