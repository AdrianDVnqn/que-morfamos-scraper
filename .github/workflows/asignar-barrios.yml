name: Asignar Barrios

on:
  workflow_dispatch:  # Solo ejecución manual

jobs:
  asignar-barrios:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Instalar dependencias básicas
        run: pip install requests
      
      # Notificación de inicio
      - name: Notificar inicio
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python -c "
          import os, requests, datetime
          webhook = os.environ.get('DISCORD_WEBHOOK_URL')
          if webhook:
              msg = f'''🏘️ **INICIANDO - Asignación de Barrios**
              📅 {datetime.datetime.now().strftime('%d/%m/%Y %H:%M')}
              
              Asignando barrios, zonas y cerca_rio a reseñas...'''
              requests.post(webhook, json={'embeds': [{'description': msg, 'color': 0x9b59b6}]})
          "
      
      - name: Instalar dependencias geoespaciales
        run: |
          pip install pandas geopandas psycopg2-binary SQLAlchemy
      
      # Ejecutar asignación de barrios (Supabase Directo)
      - name: Asignar barrios (Supabase)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python asignar_barrios.py
      
      # Notificación de éxito
      - name: Notificar éxito
        if: success()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python -c "
          import os, requests, datetime, csv
          webhook = os.environ.get('DISCORD_WEBHOOK_URL')
          if not webhook: exit(0)
          
          # Contar reseñas con barrio
          con_barrio = sin_barrio = 0
          try:
              with open('reviews_neuquen.csv', 'r') as f:
                  reader = csv.DictReader(f)
                  for row in reader:
                      if row.get('barrio'): con_barrio += 1
                      else: sin_barrio += 1
          except: pass
          
          msg = f'''✅ **COMPLETADO - Asignación de Barrios**
          📅 {datetime.datetime.now().strftime('%d/%m/%Y %H:%M')}
          
          🏘️ Reseñas con barrio: **{con_barrio}**
          ❓ Sin barrio asignado: **{sin_barrio}**'''
          
          requests.post(webhook, json={'embeds': [{'description': msg, 'color': 0x2ecc71}]})
          "
      
      # Notificación de error
      - name: Notificar error
        if: failure()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python -c "
          import os, requests, datetime
          webhook = os.environ.get('DISCORD_WEBHOOK_URL')
          if webhook:
              msg = f'''❌ **ERROR - Asignación de Barrios**
              📅 {datetime.datetime.now().strftime('%d/%m/%Y %H:%M')}
              
              Revisar logs en GitHub Actions.'''
              requests.post(webhook, json={'embeds': [{'description': msg, 'color': 0xe74c3c}]})
          "
